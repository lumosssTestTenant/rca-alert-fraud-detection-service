upstream app_service { server rca-alert-fraud-detection-service:3000; keepalive 32; }

server {
    listen 80;
    server_name _;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";
    proxy_read_timeout 30s;
    proxy_connect_timeout 5s;

    location ~ ^/(health|ready)$ { proxy_pass http://app_service; access_log off; }
    location / { proxy_pass http://app_service; client_max_body_size 10m; }
}
